# --- Secrets / env ---
.env
*.env
!*.env.example
backend/.env
*.pem

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Node / Next.js ---
node_modules/
frontend/.next/
frontend/out/
frontend/node_modules/
npm-debug.log*
yarn-error.log*

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# Phase 4 test scratch (defensive; test now uses system temp)
phase4_logs/

# Phase 9: regression_gate.py's persisted baseline is runtime state (each machine/run
# records its own), not source - a bad run (e.g. quota-exhausted judge calls) can
# corrupt it, and it should never be committed as if it were a trusted, reviewed value.
backend/evaluation/baseline_score.json
